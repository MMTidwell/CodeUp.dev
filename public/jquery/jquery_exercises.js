"use strict";

// $(document).ready(function(){}); checks to see if the DOM is ready
$(document).ready(function() {

	// // ALERT the contents of id num3 when the page is loaded
	// var contents = $('#num3').html();
	// alert(contents);

	// // ALERT the contents of class lorem when the page is loaded (this will run after the first content var)
	// var secondContent = $('.lorem1').html();
	// alert(secondContent);

	// // CSS changes the background color to yellow after the first 2 alerts
	// $(".lorem2").css('background-color', '#FF0');

	// // CSS gives border to class lorem1
	// $(".lorem1").css('border', '1px solid black');

	// // CSS changes font size for li element
	// $("li").css("font-size", "20px");

	// // CSS mulit selectors, changing the background to yellow
	// $("h1, p, li").css("background-color", "#FF0");

	// // CSS * collects everything in the HTML file and sets a red border around it
	// $("*").css("border", "1px solid #F00");

	// // CSS center aligns text for lorem2
	// $(".lorem2").css("text-align", "center");

	// // CLICK adding a click event to h1
	// $("#codeup").click(function() {
	// 	alert("h1 with id 'codeup' was clicked");
	// });
	
	// // HOVER Changes the background color when hovering over h1
	// $('#codeup').hover (
	// 	function() {
	// 		$(this).css('background-color', '#FF0');	
	// 	},
	// 	function() {
	// 		$(this).css('background-color', '#FFF');
	// 	}
	// );

	// // DOUBLE CLICK EVENT
	// $("p").dblclick(function(){
	// 	$("p").css("font-size", "18px");
	// });

	// // HOVER EVENT
	// $('li').hover (
	// 	function() {
	// 		// mouse in
	// 		$(this).css("color", "red");
	// 	},
	// 	function() {
	// 		// mouse out
	// 		$(this).css("color", "black");
	// 	}
	// );

	// // KEYDOWN this does need to be done either in a input field or the body of the doc
	// $("body").keydown(function() {
	// 	alert('.keydown() event fired');
	// });	

	// // KEYUP this is the same as keydown(), note that both doesnt need to be there.
	// $("body").keyup(function() {
	// 	alert('.keyUP() event fired');
	// });

});













